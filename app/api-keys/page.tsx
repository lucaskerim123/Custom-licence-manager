import { requireUser } from '../../lib/session';import { listApiKeys,createApiKey,revokeApiKey,ApiScope } from '../../lib/core/api-keys';import ApiKeyForm from './form';import SideNav from '../components/SideNav';export const dynamic='force-dynamic';const scopes:ApiScope[]=['license.issue','license.validate','license.manage','releases.read','releases.write','deployment.read','deployment.write'];async function create(formData:FormData){'use server';const u=await requireUser();if(!['owner','admin'].includes(u.role))return;const name=String(formData.get('name')||'').trim();const selected=String(formData.get('scopes')||'').split(',').filter((x):x is ApiScope=>scopes.includes(x as ApiScope));if(!name||!selected.length)return;const result=await createApiKey({name,scopes:selected,actorUserId:u.id});return result.key;}async function revoke(formData:FormData){'use server';const u=await requireUser();if(!['owner','admin'].includes(u.role))return;const id=String(formData.get('id')||'');if(id)await revokeApiKey(id,u.id);}export default async function ApiKeys(){const u=await requireUser();const keys=await listApiKeys();return <div className="shell"><SideNav active="settings"/><main className="main"><div className="top"><div><div className="muted" style={{textTransform:'uppercase',letterSpacing:'.08em',fontSize:11,fontWeight:800}}>System / Access</div><h1 className="title">API Access</h1><p className="muted">Machine credentials for Billing Store, installed products and release clients.</p></div><span className="badge">{u.role}</span></div><div className="section card"><h2>Create integration key</h2><p className="muted">Keys are shown once and only hashes are retained. Use separate least-privilege keys for separate integrations.</p>{['owner','admin'].includes(u.role)&&<ApiKeyForm action={create} scopes={scopes}/>}</div><div className="section"><table className="table"><thead><tr><th>Name</th><th>Key</th><th>Scopes</th><th>Status</th><th>Last used</th><th>Control</th></tr></thead><tbody>{keys.map(k=><tr key={k.id}><td><strong>{k.name}</strong></td><td className="mono">••••••••{k.key_last4}</td><td>{(k.scopes as string[]).join(', ')}</td><td><span className={`badge ${k.status==='active'?'ok':'off'}`}>{k.status}</span></td><td>{k.last_used_at?new Date(k.last_used_at).toLocaleString():'Never'}</td><td>{k.status==='active'&&['owner','admin'].includes(u.role)&&<form action={revoke}><input type="hidden" name="id" value={k.id}/><button className="button danger">Revoke</button></form>}</td></tr>)}</tbody></table></div></main></div>}