import { requireUser } from '../../lib/session';
import { listApiKeys, createApiKey, revokeApiKey, ApiScope } from '../../lib/core/api-keys';
import ApiKeyForm from './form';
export const dynamic='force-dynamic';
const scopes: ApiScope[]=['license.issue','license.validate','license.manage','releases.read','releases.write','deployment.read','deployment.write'];
async function create(formData:FormData){'use server';const user=await requireUser();if(!['owner','admin'].includes(user.role))return;const name=String(formData.get('name')||'').trim();const selected=String(formData.get('scopes')||'').split(',').filter((x):x is ApiScope=>scopes.includes(x as ApiScope));if(!name||!selected.length)return;const result=await createApiKey({name,scopes:selected,actorUserId:user.id});return result.key;}
async function revoke(formData:FormData){'use server';const user=await requireUser();if(!['owner','admin'].includes(user.role))return;const id=String(formData.get('id')||'');if(id)await revokeApiKey(id,user.id);}
export default async function ApiKeys(){const user=await requireUser();const keys=await listApiKeys();return <div className="shell"><aside className="side"><div className="brand">License Manager</div><nav className="nav"><a href="/">Overview</a><a href="/licenses">Licenses</a><a href="/products">Products</a><a href="/releases">Releases</a><a href="/users">Users</a><a href="/settings">System Settings</a><a className="active" href="/api-keys">API Keys</a><a href="/api-docs">API Contract</a></nav></aside><main className="main"><h1 className="title">API Keys</h1><p className="muted">Create separate machine-to-machine keys for Billing Store, products, deployment and release clients. Keys are shown once and only their hashes are stored.</p>{['owner','admin'].includes(user.role)&&<ApiKeyForm action={create} scopes={scopes}/>}<div className="section"><table className="table"><thead><tr><th>Name</th><th>Key</th><th>Scopes</th><th>Status</th><th>Last used</th><th></th></tr></thead><tbody>{keys.map(k=><tr key={k.id}><td>{k.name}</td><td>••••••••{k.key_last4}</td><td>{(k.scopes as string[]).join(', ')}</td><td><span className={`badge ${k.status==='active'?'ok':'off'}`}>{k.status}</span></td><td>{k.last_used_at?new Date(k.last_used_at).toLocaleString():'Never'}</td><td>{k.status==='active'&&['owner','admin'].includes(user.role)&&<form action={revoke}><input type="hidden" name="id" value={k.id}/><button className="button danger" type="submit">Revoke</button></form>}</td></tr>)}</tbody></table></div></main></div>}
