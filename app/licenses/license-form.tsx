'use client';
import { useActionState } from 'react';
import { issueLicenseAction } from './actions';
const initial={ok:false,key:'',error:''};
export default function LicenseForm({products}:{products:{id:string,name:string}[]}){const [state,action,pending]=useActionState(issueLicenseAction,initial);return <div className="section card"><form className="form" action={action}><label>Product<select className="input" name="product_id" required><option value="">Select product</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Customer external ID<input className="input" name="customer"/></label><label>External reference<input className="input" name="reference"/></label><label>Expiry<input className="input" name="expires" type="datetime-local"/></label><button className="button" disabled={pending}>{pending?'Issuing…':'Issue license'}</button></form>{state.error&&<div className="notice danger-text">{state.error}</div>}{state.ok&&state.key&&<div className="notice okBox" style={{marginTop:12}}><strong>License issued. Copy this key now.</strong><div style={{marginTop:8,fontFamily:'monospace',wordBreak:'break-all'}}>{state.key}</div><div className="muted" style={{marginTop:6}}>The plaintext key is not stored in the database.</div></div>}</div>}
