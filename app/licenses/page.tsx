import { db } from '../../lib/db';
import { requireUser } from '../../lib/session';
import { licenseControlAction } from './actions';
import LicenseForm from './license-form';
import SideNav from '../components/SideNav';
export const dynamic='force-dynamic';
const roles=['owner','admin','operator'];
export default async function Licenses(){const user=await requireUser();const [products,licenses]=await Promise.all([db().query("select id,name from products where status='active' order by name"),db().query(`select l.id,l.license_key_last4,l.status,l.customer_external_id,l.customer_override,l.external_reference,l.expires_at,p.name product,(select count(*) from activations a where a.license_id=l.id) installation_count from licenses l join products p on p.id=l.product_id order by l.created_at desc`)]);return <div className="shell"><SideNav active="licenses"/><main className="main"><h1 className="title">Licenses</h1><p className="muted">License Master is the authority. Billing Store customer numbers are shown as the external customer ID; ADMIN is reserved for explicit administrative issuance.</p><LicenseForm products={products.rows}/><div className="section"><table className="table"><thead><tr><th>Key</th><th>Product</th><th>Status</th><th>Customer</th><th>Reference</th><th>Installations</th><th>Expiry</th><th>Controls</th></tr></thead><tbody>{licenses.rows.map(l=><tr key={l.id}><td>••••-{l.license_key_last4}</td><td>{l.product}</td><td><span className={`badge ${l.status==='active'?'ok':'off'}`}>{l.status}</span></td><td>{l.customer_external_id||'—'} {l.customer_override&&<span className="badge">override</span>}</td><td>{l.external_reference||'—'}</td><td><a href={`/installations?license=${l.id}`}>{l.installation_count}</a></td><td>{l.expires_at?new Date(l.expires_at).toLocaleString():'Never'}</td><td>{roles.includes(user.role)&&<div className="actions">{l.status!=='revoked'&&<><form action={licenseControlAction}><input type="hidden" name="id" value={l.id}/><input type="hidden" name="action" value="rotate"/><button className="button secondary">Rotate</button></form><form action={licenseControlAction}><input type="hidden" name="id" value={l.id}/><input type="hidden" name="action" value={l.status==='active'?'suspend':'activate'}/><button className="button secondary">{l.status==='active'?'Suspend':'Activate'}</button></form><form action={licenseControlAction}><input type="hidden" name="id" value={l.id}/><input type="hidden" name="action" value="revoke"/><button className="button secondary">Terminate</button></form></>}</div>}</td></tr>)}</tbody></table></div></main></div>}
