import { requireUser } from '../../lib/session';
import { createProduct, listProducts, setProductStatus } from '../../lib/core/products';
export const dynamic='force-dynamic';

async function create(formData:FormData){'use server';const user=await requireUser();if(!['owner','admin'].includes(user.role))return;const name=String(formData.get('name')||'').trim();const slug=String(formData.get('slug')||'').trim().toLowerCase();if(!name||!slug)return;await createProduct({name,slug,description:String(formData.get('description')||'')||null,actorUserId:user.id,actor:user.email});}
async function status(formData:FormData){'use server';const user=await requireUser();if(!['owner','admin'].includes(user.role))return;const id=String(formData.get('id')||'');const value=String(formData.get('status')||'disabled') as 'active'|'disabled'|'archived';if(['active','disabled','archived'].includes(value))await setProductStatus(id,value,user.id,user.email);}

export default async function Products(){const user=await requireUser();const products=await listProducts();return <div className="shell"><aside className="side"><div className="brand">License Manager</div><nav className="nav"><a href="/">Overview</a><a href="/licenses">Licenses</a><a className="active" href="/products">Products</a><a href="/releases">Releases</a><a href="/users">Users</a><a href="/settings">System Settings</a><a href="/api-docs">API Contract</a></nav></aside><main className="main"><h1 className="title">Products</h1><p className="muted">Registered products are authoritative here. Installed products validate licenses against this system's API.</p>{['owner','admin'].includes(user.role)&&<div className="section card"><form className="form" action={create}><label>Name<input className="input" name="name" required/></label><label>Slug<input className="input" name="slug" placeholder="orbitfs" pattern="[a-z0-9][a-z0-9._-]*" required/></label><label>Description<input className="input" name="description"/></label><button className="button" type="submit">Create product</button></form></div>}<div className="section"><table className="table"><thead><tr><th>Name</th><th>Slug</th><th>Status</th><th>Created</th><th></th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.name}</td><td>{p.slug}</td><td><span className={`badge ${p.status==='active'?'ok':'off'}`}>{p.status}</span></td><td>{new Date(p.created_at).toLocaleString()}</td><td>{['owner','admin'].includes(user.role)&&p.status!=='archived'&&<form action={status}><input type="hidden" name="id" value={p.id}/><input type="hidden" name="status" value={p.status==='active'?'disabled':'active'}/><button className="button secondary">{p.status==='active'?'Disable':'Enable'}</button></form>}</td></tr>)}</tbody></table></div></main></div>}
