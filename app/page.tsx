import Link from 'next/link';
import { db } from '../lib/db';
import { requireUser } from '../lib/session';
import SideNav from './components/SideNav';
import LiveRefresh from './components/LiveRefresh';
export const dynamic='force-dynamic';
async function stats(){const p=db();const [licenses,products,releases,settings,users,pending,failed,activations]=await Promise.all([p.query("select count(*)::int count from licenses"),p.query("select count(*)::int count from products where status <> 'archived'"),p.query("select count(*)::int count from releases where status='published'"),p.query("select system_enabled,licensing_enabled,maintenance_mode,release_system_enabled,deployment_enabled from system_settings where id=true"),p.query("select count(*)::int count from users where status='active'"),p.query("select count(*)::int count from releases where status <> 'published' and review_status='pending'"),p.query("select count(*)::int count from releases where coalesce((manifest->'validation'->>'status'),'not_run')='failed'"),p.query("select count(*)::int count from activations where status='active'")]);return{licenses:licenses.rows[0].count,products:products.rows[0].count,releases:releases.rows[0].count,users:users.rows[0].count,settings:settings.rows[0],pending:pending.rows[0].count,failed:failed.rows[0].count,activations:activations.rows[0].count};}
export default async function Home(){await requireUser();const s=await stats();const online=Boolean(s.settings?.system_enabled);const switches=[['Licensing',s.settings?.licensing_enabled],['Release system',s.settings?.release_system_enabled],['Deployment',s.settings?.deployment_enabled]];return <div className="shell"><SideNav active="overview"/><main className="main"><div className="top"><div><div className="muted" style={{textTransform:'uppercase',letterSpacing:'.08em',fontSize:11,fontWeight:800}}>Authority Control Plane</div><h1 className="title">Operations Overview</h1><div className="muted">Control licensing, validation, releases and deployment authority from one place.</div></div><div className="actions"><LiveRefresh/><span className={`badge ${online?'ok':'off'}`}>{online?'Authority online':'Authority offline'}</span></div></div><div className="grid"><div className="card"><div className="muted">Licenses</div><div className="metric">{s.licenses}</div><Link href="/licenses">Manage licensing →</Link></div><div className="card"><div className="muted">Active installations</div><div className="metric">{s.activations}</div><Link href="/installations">Inspect installations →</Link></div><div className="card"><div className="muted">Release candidates</div><div className="metric">{s.pending}</div><Link href="/releases">Open release queue →</Link></div><div className="card"><div className="muted">Validation failures</div><div className="metric">{s.failed}</div><Link href="/releases">Review failures →</Link></div></div><div className="detail-grid section"><section className="card"><h2>System posture</h2><div style={{display:'grid',gap:8}}>{switches.map(([label,value])=><div key={String(label)} className="notice" style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span>{String(label)}</span><span className={`badge ${value?'ok':'off'}`}>{value?'Enabled':'Disabled'}</span></div>)}</div><div className="notice" style={{marginTop:10}}><strong>Authority boundary</strong><div className="muted" style={{marginTop:4}}>License Manager owns licenses, validation decisions, activations, release state and deployment metadata. Billing Store remains the customer commerce/publication layer.</div></div></section><section className="card"><h2>Operator shortcuts</h2><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:8}}><Link className="button" href="/releases/base">Base Deployment</Link><Link className="button" href="/releases">Release Updates</Link><Link className="button secondary" href="/licenses">Licensing</Link><Link className="button secondary" href="/settings">System Settings</Link></div><p className="muted" style={{marginTop:14,fontSize:12}}>Technical approval never publishes directly to customers; approved candidates remain ready for the Billing Store final publication gate.</p></section></div></main></div>}