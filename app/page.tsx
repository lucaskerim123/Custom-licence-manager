import Link from 'next/link';
import { db } from '../lib/db';
import { requireUser } from '../lib/session';
export const dynamic = 'force-dynamic';
async function stats(){const pool=db();const [licenses,products,releases,settings]=await Promise.all([pool.query("select count(*)::int as count from licenses"),pool.query("select count(*)::int as count from products where status <> 'archived'"),pool.query("select count(*)::int as count from releases where status='published'"),pool.query("select system_enabled,licensing_enabled,maintenance_mode from system_settings where id=true")]);return {licenses:licenses.rows[0].count,products:products.rows[0].count,releases:releases.rows[0].count,settings:settings.rows[0]};}
export default async function Home(){await requireUser();const s=await stats();return <div className="shell"><aside className="side"><div className="brand">License Manager</div><nav className="nav"><Link className="active" href="/">Overview</Link><Link href="/licenses">Licenses</Link><Link href="/products">Products</Link><Link href="/releases">Releases</Link><Link href="/settings">System Settings</Link><Link href="/api-docs">API Contract</Link></nav><form action="/api/auth/logout" method="post"><button className="nav-button" type="submit">Sign out</button></form></aside><main className="main"><div className="top"><div><h1 className="title">Control Plane</h1><div className="muted">Independent authority for licensing, validation, base deployment and releases.</div></div><span className={`badge ${s.settings.system_enabled&&s.settings.licensing_enabled?'ok':'off'}`}>{s.settings.system_enabled&&s.settings.licensing_enabled?'Operational':'Restricted'}</span></div><div className="grid"><div className="card"><div className="muted">Licenses</div><div className="metric">{s.licenses}</div></div><div className="card"><div className="muted">Products</div><div className="metric">{s.products}</div></div><div className="card"><div className="muted">Published releases</div><div className="metric">{s.releases}</div></div><div className="card"><div className="muted">Maintenance</div><div className="metric">{s.settings.maintenance_mode?'ON':'OFF'}</div></div></div><div className="section"><h2>Authority boundary</h2><div className="notice">This application owns its users, settings, license issuance, status, validation decisions, deployment metadata, release metadata and authority controls. External systems connect to it through the integration API; no external billing system is required for the manager to operate.</div></div></main></div>}
