import { NextResponse } from 'next/server';
import { integrationAuthorized } from '../../../../lib/auth';
import { issueLicense } from '../../../../lib/core/licenses';
import { db } from '../../../../lib/db';
export async function POST(request:Request){const auth=await integrationAuthorized(request,'license.issue');if(!auth)return NextResponse.json({error:'Unauthorized'},{status:401});const body=await request.json().catch(()=>null);const productId=String(body?.product_id||'');if(!productId)return NextResponse.json({error:'product_id is required'},{status:400});const settings=(await db().query('select system_enabled,licensing_enabled,maintenance_mode from system_settings where id=true')).rows[0];if(!settings?.system_enabled||!settings?.licensing_enabled||settings.maintenance_mode)return NextResponse.json({error:'License authority unavailable'},{status:503});const product=(await db().query("select id from products where id=$1 and status='active'",[productId])).rows[0];if(!product)return NextResponse.json({error:'Product not found or disabled'},{status:404});try{const result=await issueLicense({productId,customerExternalId:body?.customer_external_id,externalReference:body?.external_reference,expiresAt:body?.expires_at?new Date(body.expires_at):null,actor:`api:${auth.name}`,metadata:body?.metadata});return NextResponse.json({license_key:result.key,license_id:result.id,status:result.status,issued_at:result.issued_at,expires_at:result.expires_at});}catch{return NextResponse.json({error:'Unable to issue license'},{status:500});}}
