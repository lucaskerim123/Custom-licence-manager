import { requireUser } from '../../lib/session';
export const dynamic='force-dynamic';
export default async function ApiDocs(){await requireUser();return <div className="shell"><aside className="side"><div className="brand">License Manager</div><nav className="nav"><a href="/">Overview</a><a href="/licenses">Licenses</a><a href="/products">Products</a><a href="/releases">Releases</a><a href="/settings">System Settings</a><a className="active" href="/api-docs">API Contract</a></nav></aside><main className="main"><h1 className="title">External API Contract</h1><p className="muted">The API is the external integration boundary. The License Manager's own UI and database do not depend on Billing Store or any external service.</p><div className="section card"><h2>Authentication</h2><pre>{`Authorization: Bearer INTEGRATION_API_TOKEN`}</pre></div><div className="section card"><h2>Issue license</h2><pre>{`POST /api/license/issue\nBody: { product_id, customer_external_id?, external_reference?, expires_at?, metadata? }\nResponse: { license_key, license_id, status, issued_at, expires_at }`}</pre></div><div className="section card"><h2>Validate license</h2><pre>{`POST /api/license/validate\nBody: { license_key, product, installation_id?, product_version? }\nResponse: { valid, code, expires_at, metadata }`}</pre></div><div className="section card"><h2>Base deployment</h2><pre>{`GET /api/deployment/base?product=orbitfs\nReturns the latest published base release for the product.`}</pre></div><div className="section card"><h2>Release updater</h2><pre>{`GET  /api/releases\nPOST /api/releases\nrelease_type: base | update\nchannel: stable | beta | ...`}</pre></div></main></div>}
